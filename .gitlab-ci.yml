image: gitlab-registry.proxym-group.net/docker/node:16.17.0

pages:
    stage: deploy
    tags:
      - docker
    script:
      - yarn install
      - yarn bootstrap # added bootstrap step
      - yarn build-storybook
      - mv libs/components/storybook-static ./public
    artifacts:
        paths:
        - public
    only:
      - master
