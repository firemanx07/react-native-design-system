image: gitlab-registry.proxym-group.net/docker/node:16.17.0

stages:
  - Prepare
  - Code_Review
  - deploy

prepare_cache:
  stage: Prepare
  tags:
    - docker
  script:
    - echo "Checking if yarn.lock has changed in the past day..."
    - if test `find ./yarn.lock -mtime +0` && test ! `find ./yarn.lock -mtime -1`; then echo "yarn.lock file has not changed in the past day. Caching node_modules..."; yarn install --frozen-lockfile; fi
  cache:
    key: "${CI_COMMIT_REF_SLUG}-node-modules"
    paths:
      - node_modules
  only:
    refs:
      - master

sonarqube-check:
  stage: Code_Review
  tags:
    - docker
  image:
    name: gitlab-registry.proxym-group.net/docker/sonar-scanner:4.8
    entrypoint: [""]
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"
    GIT_DEPTH: "0"
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
      - node_modules
  script:
    - sonar-scanner -Dsonar.host.url="$SONAR_HOST_URL" -Dsonar.login="$SONAR_TOKEN"
  allow_failure: true
  only:
    refs:
      - master


test_CI:
  stage: Code_Review
  tags:
    - docker
  script:
    - yarn install
    - yarn lint
    - yarn test:ci
  only:
    refs:
      - merge_requests
    variables:
      - $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == $CI_DEFAULT_BRANCH

pages:
  stage: deploy
  tags:
    - docker
  script:
    - yarn install
    - yarn bootstrap
    - export NODE_ENV=production
    - yarn build-storybook
    - mv libs/components/storybook-static ./public
  artifacts:
    paths:
      - public
  only:
    refs:
      - master
    changes:
      - "libs/components/src/**/*.stories.tsx"
  when: manual 
